import 'package:flutter/material.dart';

class CommunityResource extends StatelessWidget {
  const CommunityResource({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}